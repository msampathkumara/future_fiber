import 'package:flutter/material.dart';
import 'package:smartwind_future_fibers/Mobile/V/Home/Tickets/ProductionPool/TicketList.dart';

class ProductionPool extends StatefulWidget {
  const ProductionPool({Key? key}) : super(key: key);

  @override
  _ProductionPoolState createState() {
    return _ProductionPoolState();
  }
}

class _ProductionPoolState extends State<ProductionPool> {
  @override
  void initState() {
    super.initState();
  }

  @override
  void dispose() {
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return const TicketList();
  }
}
